#include<stdio.h>
#include<conio.h>
void main()
{
    printf("enter the value");
    scanf("%d",&n);
    if(n%2==0)
    {
        printf("%d",even);
    }
    else
        if(n%2!=0)
    {
        printf("%d",odd);
    }
    else
        printf("Invalid");
}
